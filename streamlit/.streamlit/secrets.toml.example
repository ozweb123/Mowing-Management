# Copy to secrets.toml locally, or set in Streamlit Community Cloud → Settings → Secrets
pin = "2468"
session_secret = "change-me-to-a-long-random-string-at-least-32-chars"
weather_lat = "39.0375"
weather_lon = "-95.7250"
weather_timezone = "America/Chicago"

# Optional — push schedule into Miles' iCloud Calendar (CalDAV)
# Create an app-specific password at https://appleid.apple.com
# (Sign-In and Security → App-Specific Passwords). Never use the normal Apple password.
# icloud_apple_id = "miles@icloud.com"
# icloud_app_password = "xxxx-xxxx-xxxx-xxxx"
